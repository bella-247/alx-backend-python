apiVersion: v1
kind: Service
metadata:
  name: messaging-service
spec:
  type: ClusterIP
  selector:
    app: messaging-app
    version: blue   # Start pointing to blue
  ports:
    - port: 8000
      targetPort: 8000
